- [Bugfix] Point the `SITE_ID` Django site at the LMS/CMS domain during initialisation, by renaming the auto-generated "example.com" site in place, so that code paths that run outside of an HTTP request (such as bulk emails sent from Celery workers) use the correct domain and branding instead of "example.com". Existing installations that already have both an "example.com" site and a site for their domain are left untouched, with a warning explaining how to consolidate them. (by @ahmed-arb)
