- [Improvement] Publish Tutor Main to PyPI as development releases of the upcoming version, on every merge to the main branch (for instance `23.0.0.dev4821`), so that plugin developers can depend on unreleased Tutor with `tutor>=23.0.0.dev0,<24.0.0.dev0`, or on one specific change with `tutor>=23.0.0.dev4821`. Pip ignores pre-releases unless they are explicitly requested, so `pip install tutor` still installs the latest actual release. (by @ahmed-arb)
